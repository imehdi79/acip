# Where editor-server runs. Baked in at build time by Vite.
# Dev default (unset): http://localhost:3000. Empty = same origin.
# For the Cloudflare deployment, point this at your VPS, e.g.:
# VITE_EDITOR_SERVER_URL=https://api.your-domain.com
VITE_EDITOR_SERVER_URL=
